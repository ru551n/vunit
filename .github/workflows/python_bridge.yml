name: 'python bridge'

# Tests of the VHDL Python integration, add_vhdl_builtins(python=True): Linux
# (bridge compiled locally), Windows (prebuilt MSVC DLLs), packaging, and that
# normal development is unaffected when the feature is not used.

on:
  push:
  pull_request:
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:

#
# Normal development, without NumPy or bridge binaries
#

  normal-dev:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    name: '🧪 Normal development · ${{ matrix.os }}'
    steps:

    - name: '🧰 Checkout'
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: '🐍 Setup Python'
      uses: actions/setup-python@v6
      with:
        python-version: '3.13'

    - name: '⚙️ Setup GHDL'
      uses: ghdl/setup-ghdl@main
      with:
        backend: mcode

    - name: '🐍 Install VUnit'
      run: |
        python -m pip install -e . pytest --progress-bar off
        ! python -c "import numpy"
        test -z "$(find vunit/python_bridge vunit/vhdl/python_bridge -name '*.so' -o -name '*.dll')"

    - name: '🚧 Unit tests'
      run: python -m pytest -ra tests/unit

    - name: '🚧 Builtins without Python support'
      env:
        VUNIT_SIMULATOR: ghdl
      run: |
        python vunit/vhdl/string_ops/run.py -p 4 -o normal_out
        test ! -e normal_out/python_bridge

#
# Linux, bridge compiled on first use
#

  linux:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - { py: '3.10', sim: nvc }
          - { py: '3.14', sim: nvc }
          - { py: '3.10', sim: ghdl, backend: mcode }
          - { py: '3.14', sim: ghdl, backend: mcode }
          - { py: '3.14', sim: ghdl, backend: llvm }
    name: '🐧 Ubuntu · Python ${{ matrix.py }} · ${{ matrix.sim }} ${{ matrix.backend }}'
    env:
      VUNIT_SIMULATOR: ${{ matrix.sim }}
      VENV: ${{ runner.temp }}/venv with space
      OUT: ${{ runner.temp }}/out dir
    steps:

    - name: '🧰 Checkout'
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: '🐍 Setup Python'
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.py }}

    - name: '⚙️ Setup NVC'
      if: matrix.sim == 'nvc'
      run: |
        curl -fsSL -o nvc.deb https://github.com/nickg/nvc/releases/download/r1.22.1/nvc_1.22.1-1_amd64_ubuntu-24.04.deb
        sudo apt-get install -y ./nvc.deb

    - name: '⚙️ Setup GHDL'
      if: matrix.sim == 'ghdl'
      uses: ghdl/setup-ghdl@main
      with:
        backend: ${{ matrix.backend }}

    - name: '🐍 Install VUnit and NumPy in a virtual environment'
      run: |
        python -m venv "$VENV"
        "$VENV/bin/python" -m pip install numpy -e . --progress-bar off

    - name: '🚧 Feature tests'
      run: '"$VENV/bin/python" vunit/vhdl/python_bridge/run.py -p 4 -o "$OUT"'

    - name: '🚧 Feature tests with cached bridge'
      run: |
        before="$(ls -l --time-style=full-iso "$OUT"/python_bridge/*/libvunit_python_bridge.so)"
        "$VENV/bin/python" vunit/vhdl/python_bridge/run.py -p 4 -o "$OUT"
        test "$before" = "$(ls -l --time-style=full-iso "$OUT"/python_bridge/*/libvunit_python_bridge.so)"

    - name: '🚧 Example'
      run: '"$VENV/bin/python" examples/vhdl/python/run.py -p 4'

#
# Windows, prebuilt MSVC DLLs
#

  dlls:
    uses: ./.github/workflows/python_bridge_dlls.yml

  windows:
    needs: dlls
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        py: ['3.10', '3.12', '3.14']
        sim: [nvc, ghdl]
    name: '🟦 Windows · Python ${{ matrix.py }} · ${{ matrix.sim }}'
    env:
      VUNIT_SIMULATOR: ${{ matrix.sim }}
      VENV: ${{ runner.temp }}/venv with space
      OUT: ${{ runner.temp }}/out dir
    steps:

    - name: '🧰 Checkout'
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: '📥 Download bridge DLLs'
      uses: actions/download-artifact@v4
      with:
        name: python-bridge-dlls
        path: vunit/python_bridge/bin

    - name: '🐍 Setup Python'
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.py }}
        architecture: x64

    - name: '⚙️ Setup NVC'
      if: matrix.sim == 'nvc'
      shell: pwsh
      run: |
        Invoke-WebRequest -Uri https://github.com/nickg/nvc/releases/download/r1.22.1/nvc-1.22.1.msi -OutFile nvc.msi
        Start-Process msiexec.exe -Wait -ArgumentList '/i', 'nvc.msi', '/qn', '/norestart'
        $bin = Get-ChildItem -Path 'C:\Program Files*' -Recurse -Filter nvc.exe -ErrorAction SilentlyContinue | Select-Object -First 1
        $bin.DirectoryName | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

    - name: '⚙️ Setup GHDL'
      if: matrix.sim == 'ghdl'
      uses: ghdl/setup-ghdl@main
      with:
        backend: mcode

    - name: '🐍 Install VUnit and NumPy in a virtual environment'
      run: |
        python -m venv "$VENV"
        "$VENV/Scripts/python" -m pip install numpy -e . --progress-bar off

    - name: '🚧 Feature tests (no compiler on PATH)'
      run: |
        ! command -v cl
        "$VENV/Scripts/python" vunit/vhdl/python_bridge/run.py -p 4 -o "$OUT"

    - name: '🚧 Example'
      run: '"$VENV/Scripts/python" examples/vhdl/python/run.py -p 4'

#
# Packaging, sources and DLLs included, no Linux binaries
#

  packaging:
    needs: dlls
    runs-on: ubuntu-latest
    name: '📦 Packaging'
    steps:

    - name: '🧰 Checkout'
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: '📥 Download bridge DLLs'
      uses: actions/download-artifact@v4
      with:
        name: python-bridge-dlls
        path: vunit/python_bridge/bin

    - name: '🐍 Setup Python'
      uses: actions/setup-python@v6
      with:
        python-version: '3.13'

    - name: '📦 Build sdist'
      run: |
        pip install -U setuptools wheel --progress-bar off
        python setup.py sdist

    - name: '🔍 Check sdist content'
      run: |
        tar tzf dist/*.tar.gz > content.txt
        for file in python_bridge/bridge.py python_bridge/runtime.py python_bridge/native/vunit_python_bridge.c \
                    vhdl/python_bridge/src/python_ffi_pkg.vhd.in vhdl/python_bridge/src/python_pkg.vhd; do
          grep -q "vunit/$file\$" content.txt || { echo "missing $file"; exit 1; }
        done
        test "$(grep -c 'vunit/python_bridge/bin/vunit_python_bridge-cp3[0-9]*-win_amd64.dll$' content.txt)" = 5
        ! grep -q '\.so$' content.txt
